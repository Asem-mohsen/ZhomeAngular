import { JoinWithAndPipe } from './join-with-and.pipe';

describe('JoinWithAndPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinWithAndPipe();
    expect(pipe).toBeTruthy();
  });
});
